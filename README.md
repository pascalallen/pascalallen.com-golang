# React | MySQL | Go -- Web App

Requires Go, Dep, and Node to be installed.

`git fetch && git checkout https://github.com/pascalallen/pascalallen.com-golang.git`
`cd server/src/app`
`dep init && dep ensure`
